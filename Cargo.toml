[package]
name = "moxui"
version = "0.1.0"
edition = "2024"

[dependencies]
wgpu = { version = "27.0.1", default-features = false, features = ["wgsl", "vulkan", "gles"] }

[dev-dependencies]
pollster = "0.4.0"
winit = "0.30.5"
